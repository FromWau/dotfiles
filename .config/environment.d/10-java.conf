# Ghostty is D-Bus activated into a systemd user unit, so it inherits that
# manager's environment, not Hyprland's — hl.env never reaches it or its shells.
# AWT assumes a reparenting WM; without this a Java window ignores resizes.
_JAVA_AWT_WM_NONREPARENTING=1
